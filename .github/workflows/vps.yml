name: VPS Linux

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Info Sistem
        run: |
          echo " VM GitHub berhasil jalan!"
          uname -a
          lsb_release -a
          free -h
          df -h
